<Window x:Class="CRK_Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRK_Browser"
        xmlns:controls="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="CRK Browser"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">
    <Window.Resources>
        <!-- 关闭按钮样式 -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextLightColor}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#ff4757" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标签页样式 -->
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" 
                                    Background="{StaticResource BackgroundColor}" 
                                    BorderBrush="{StaticResource BorderColor}" 
                                    BorderThickness="1,1,1,0" 
                                    CornerRadius="6,6,0,0" 
                                    Margin="-1,0,-1,0" 
                                    Padding="16,8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="TabTitle" 
                                               Text="{TemplateBinding Header}" 
                                               Margin="0,0,8,0" 
                                               MaxWidth="150" 
                                               TextTrimming="CharacterEllipsis"/>
                                    <Button x:Name="CloseButton" 
                                            Content="×" 
                                            Width="20" 
                                            Height="20" 
                                            HorizontalAlignment="Right" 
                                            VerticalAlignment="Center" 
                                            Visibility="Collapsed" 
                                            Click="CloseTabButton_Click" 
                                            Tag="{TemplateBinding Tag}" 
                                            Style="{StaticResource CloseButtonStyle}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundColor}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,2,1,0"/>
                                <Setter TargetName="CloseButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseButton" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryLightColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 地址栏样式 -->
        <Style x:Key="AddressBarStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#f8f9fa" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="24">
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock x:Name="Icon" 
                                               Text="🔗" 
                                               Margin="0,0,12,0" 
                                               FontSize="16"/>
                                    <TextBlock x:Name="Placeholder" 
                                               Text="搜索或输入网址" 
                                               Foreground="{StaticResource TextLightColor}" 
                                               IsHitTestVisible="False" 
                                               Visibility="Collapsed"/>
                                    <ScrollViewer Margin="0" 
                                                  x:Name="PART_ContentHost" 
                                                  VerticalAlignment="Center" 
                                                  HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}" />
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundColor}" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 导航按钮样式 -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryLightColor}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="1" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryColor}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextLightColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- 标题栏 -->
        <Grid Height="32" Background="{StaticResource TitleBarColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                <Image Source="Resources/app.ico" Width="24" Height="24" Margin="0,0,8,0"/>
                <TextBlock Text="CRK Browser" Foreground="White" FontSize="14" FontWeight="Medium"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                <Button x:Name="MinimizeButton" Content="−" Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="MinimizeButton_Click"/>
                <Button x:Name="MaximizeButton" Content="□" Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="MaximizeButton_Click"/>
                <Button x:Name="CloseButton" Content="×" Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- 工具栏 -->
        <Border Height="56" Margin="0,32,0,0" Background="{StaticResource BackgroundColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 导航按钮 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                    <Button x:Name="BackButton" Style="{StaticResource NavButtonStyle}" Content="←" Click="BackButton_Click"/>
                    <Button x:Name="ForwardButton" Style="{StaticResource NavButtonStyle}" Content="→" Click="ForwardButton_Click"/>
                    <Button x:Name="RefreshButton" Style="{StaticResource NavButtonStyle}" Content="⟳" Click="RefreshButton_Click"/>
                    <Button x:Name="HomeButton" Style="{StaticResource NavButtonStyle}" Content="🏠" Click="HomeButton_Click"/>
                </StackPanel>
                
                <!-- 地址栏 -->
                <TextBox x:Name="AddressBar" Grid.Column="1" Style="{StaticResource AddressBarStyle}" KeyDown="AddressBar_KeyDown"/>
                
                <!-- 右侧按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Button x:Name="BookmarksButton" Style="{StaticResource NavButtonStyle}" Content="★" Click="BookmarksButton_Click"/>
                    <Button x:Name="HistoryButton" Style="{StaticResource NavButtonStyle}" Content="🕒" Click="HistoryButton_Click"/>
                    <Button x:Name="DownloadsButton" Style="{StaticResource NavButtonStyle}" Content="↓" Click="DownloadsButton_Click"/>
                    <Button x:Name="SettingsButton" Style="{StaticResource NavButtonStyle}" Content="⚙" Click="SettingsButton_Click"/>
                    <Button x:Name="LanguageButton" Style="{StaticResource NavButtonStyle}" Content="🌐" Click="LanguageButton_Click"/>
                    <Button x:Name="UserButton" Style="{StaticResource NavButtonStyle}" Content="👤" Click="UserButton_Click"/>
                    <Button x:Name="NewTabButton" Style="{StaticResource NavButtonStyle}" Content="+" Click="NewTabButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 标签页控件 -->
        <TabControl x:Name="TabControl" Margin="0,88,0,32" 
                    Background="{StaticResource BackgroundColor}" 
                    BorderBrush="{StaticResource BorderColor}" 
                    BorderThickness="0,1,0,0" 
                    SelectionChanged="TabControl_SelectionChanged">
            <!-- 标签页内容将在代码中动态添加 -->
        </TabControl>
        
        <!-- 状态栏 -->
        <Border Height="36" Margin="0,0,0,0" VerticalAlignment="Bottom" Background="{StaticResource BackgroundColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Margin="12,0,0,0" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource TextLightColor}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,12,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <TextBlock x:Name="SecurityStatus" Margin="4" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource AccentColor}"/>
                    <TextBlock x:Name="ConnectionStatus" Margin="4" VerticalAlignment="Center" FontSize="13" Foreground="{StaticResource TextLightColor}" Text="在线"/>
                </StackPanel>
                <ProgressBar x:Name="LoadProgress" Width="120" Height="6" Margin="4" Visibility="Collapsed" 
                             Background="{StaticResource BorderColor}" 
                             Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Grid>
        </Border>
    </Grid>
</Window>