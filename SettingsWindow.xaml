<Window x:Class="CRK_Browser.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRK_Browser"
        mc:Ignorable="d"
        Title="设置"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">
    <Window.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}" />
            <Setter Property="Margin" Value="0,16,0,8" />
        </Style>
        
        <Style x:Key="SettingLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
        
        <Style x:Key="SettingControlStyle" TargetType="Control">
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Width" Value="200" />
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkColor}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1557b0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#cccccc" />
                                <Setter Property="Foreground" Value="#999999" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- 标题栏 -->
        <Grid Height="32" Background="{StaticResource PrimaryColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="CRK Browser 设置" Foreground="White" FontSize="14" FontWeight="Medium" Margin="12,0,0,0" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="×" Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="CloseButton_Click"/>
        </Grid>
        
        <!-- 内容区域 -->
        <Grid Margin="0,32,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- 左侧导航 -->
            <Grid Grid.Column="0" Background="{StaticResource PrimaryLightColor}">
                <StackPanel Margin="12">
                    <Button x:Name="GeneralButton" Content="常规" Click="GeneralButton_Click" Style="{StaticResource ButtonStyle}" Margin="0,0,0,8"/>
                    <Button x:Name="PrivacyButton" Content="隐私" Click="PrivacyButton_Click" Style="{StaticResource ButtonStyle}" Margin="0,0,0,8"/>
                    <Button x:Name="AppearanceButton" Content="外观" Click="AppearanceButton_Click" Style="{StaticResource ButtonStyle}" Margin="0,0,0,8"/>
                    <Button x:Name="AdvancedButton" Content="高级" Click="AdvancedButton_Click" Style="{StaticResource ButtonStyle}" Margin="0,0,0,8"/>
                </StackPanel>
            </Grid>
            
            <!-- 右侧内容 -->
            <Grid Grid.Column="1">
                <ScrollViewer Margin="24">
                    <StackPanel>
                        <!-- 常规设置 -->
                        <StackPanel x:Name="GeneralSettings" Visibility="Visible">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="常规设置"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="默认主页:"/>
                                <TextBox Grid.Column="1" x:Name="HomePageTextBox" Style="{StaticResource SettingControlStyle}" Text="https://www.bing.com"/>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="默认搜索引擎:"/>
                                <ComboBox Grid.Column="1" x:Name="SearchEngineComboBox" Style="{StaticResource SettingControlStyle}">
                                    <ComboBoxItem Content="Bing" IsSelected="True"/>
                                    <ComboBoxItem Content="Google"/>
                                    <ComboBoxItem Content="Baidu"/>
                                </ComboBox>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="启动时:"/>
                                <ComboBox Grid.Column="1" x:Name="StartupComboBox" Style="{StaticResource SettingControlStyle}">
                                    <ComboBoxItem Content="打开新标签页" IsSelected="True"/>
                                    <ComboBoxItem Content="继续上次的会话"/>
                                    <ComboBoxItem Content="打开主页"/>
                                </ComboBox>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="标签页设置:"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <CheckBox x:Name="NewTabHomeCheckBox" Content="新标签页打开主页" IsChecked="True"/>
                                    <CheckBox x:Name="CloseTabWarningCheckBox" Content="关闭多个标签页时警告" IsChecked="True"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        
                        <!-- 隐私设置 -->
                        <StackPanel x:Name="PrivacySettings" Visibility="Collapsed">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="隐私设置"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="历史记录:"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <CheckBox x:Name="SaveHistoryCheckBox" Content="保存浏览历史" IsChecked="True"/>
                                    <Button x:Name="ClearHistoryButton" Content="清除历史记录" Click="ClearHistoryButton_Click" Width="120"/>
                                </StackPanel>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="缓存:"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <CheckBox x:Name="UseCacheCheckBox" Content="使用缓存" IsChecked="True"/>
                                    <Button x:Name="ClearCacheButton" Content="清除缓存" Click="ClearCacheButton_Click" Width="120"/>
                                </StackPanel>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="Cookie:"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <CheckBox x:Name="AcceptCookiesCheckBox" Content="接受Cookie" IsChecked="True"/>
                                    <Button x:Name="ClearCookiesButton" Content="清除Cookie" Click="ClearCookiesButton_Click" Width="120"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        
                        <!-- 外观设置 -->
                        <StackPanel x:Name="AppearanceSettings" Visibility="Collapsed">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="外观设置"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="主题:"/>
                                <ComboBox Grid.Column="1" x:Name="ThemeComboBox" Style="{StaticResource SettingControlStyle}">
                                    <ComboBoxItem Content="浅蓝色" IsSelected="True"/>
                                    <ComboBoxItem Content="深蓝色"/>
                                </ComboBox>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="字体大小:"/>
                                <ComboBox Grid.Column="1" x:Name="FontSizeComboBox" Style="{StaticResource SettingControlStyle}">
                                    <ComboBoxItem Content="小"/>
                                    <ComboBoxItem Content="中" IsSelected="True"/>
                                    <ComboBoxItem Content="大"/>
                                </ComboBox>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="显示书签栏:"/>
                                <CheckBox Grid.Column="1" x:Name="ShowBookmarksBarCheckBox" Content="在标签页下方显示书签栏" IsChecked="False"/>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="显示状态栏:"/>
                                <CheckBox Grid.Column="1" x:Name="ShowStatusBarCheckBox" Content="在窗口底部显示状态栏" IsChecked="True"/>
                            </Grid>
                        </StackPanel>
                        
                        <!-- 高级设置 -->
                        <StackPanel x:Name="AdvancedSettings" Visibility="Collapsed">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="高级设置"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="WebView2路径:"/>
                                <TextBox Grid.Column="1" x:Name="WebView2PathTextBox" Style="{StaticResource SettingControlStyle}" IsReadOnly="True" Text="默认路径"/>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="用户数据路径:"/>
                                <TextBox Grid.Column="1" x:Name="UserDataPathTextBox" Style="{StaticResource SettingControlStyle}" IsReadOnly="True" Text="默认路径"/>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="性能:"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <CheckBox x:Name="HardwareAccelerationCheckBox" Content="启用硬件加速" IsChecked="True"/>
                                    <CheckBox x:Name="ProcessPerSiteCheckBox" Content="每个站点使用单独进程" IsChecked="False"/>
                                </StackPanel>
                                
                                <Label Grid.Column="0" Style="{StaticResource SettingLabelStyle}" Content="开发者工具:"/>
                                <CheckBox Grid.Column="1" x:Name="DeveloperToolsCheckBox" Content="启用开发者工具" IsChecked="True"/>
                            </Grid>
                        </StackPanel>
                        
                        <!-- 底部按钮 -->
                        <StackPanel Orientation="Horizontal" Margin="0,24,0,0" HorizontalAlignment="Right">
                            <Button x:Name="CancelButton" Content="取消" Click="CancelButton_Click" Style="{StaticResource ButtonStyle}" />
                            <Button x:Name="SaveButton" Content="保存" Click="SaveButton_Click" Style="{StaticResource ButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>