<Window x:Class="CRK_Browser.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRK_Browser"
        mc:Ignorable="d"
        Title="用户登录"
        Height="400"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">
    <Window.Resources>
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}" />
            <Setter Property="Margin" Value="0,0,0,20" />
        </Style>
        
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="240" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="240" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
        
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Width" Value="240" />
            <Setter Property="Height" Value="28" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- 标题栏 -->
        <Grid Height="32" Background="{StaticResource PrimaryColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="CRK-Browser" Foreground="White" FontSize="14" FontWeight="Medium" Margin="12,0,0,0" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="×" Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" Foreground="White" Click="CloseButton_Click"/>
        </Grid>
        
        <!-- 内容区域 -->
        <Grid Margin="0,32,0,0" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- 标题 -->
                <TextBlock Style="{StaticResource TitleTextStyle}" Text="用户登录" HorizontalAlignment="Center"/>
                
                <!-- 用户名 -->
                <Label Style="{StaticResource LabelStyle}" Content="用户名:" HorizontalAlignment="Left"/>
                <TextBox x:Name="UsernameTextBox" Style="{StaticResource TextBoxStyle}" MaxLength="20"/>
                
                <!-- 密码 -->
                <Label Style="{StaticResource LabelStyle}" Content="密码:" HorizontalAlignment="Left"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource TextBoxStyle}" MaxLength="20"/>
                
                <!-- 错误信息 -->
                <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="12" Margin="0,0,0,12" HorizontalAlignment="Center" TextWrapping="Wrap" Visibility="Collapsed"/>
                
                <!-- 登录按钮 -->
                <Button x:Name="LoginButton" Style="{StaticResource ButtonStyle}" Content="登录" Click="LoginButton_Click"/>
                
                <!-- 注册按钮 -->
                <Button x:Name="RegisterButton" Style="{StaticResource ButtonStyle}" Content="注册" Click="RegisterButton_Click"/>
                
                <!-- 分割线 -->
                <Grid Width="240" Height="1" Background="{StaticResource BorderColor}" Margin="0,12,0,12"/>
                
                <!-- 其他选项 -->
                <Button x:Name="ForgotPasswordButton" Style="{StaticResource LinkButtonStyle}" Content="忘记密码?" Click="ForgotPasswordButton_Click"/>
                <Button x:Name="GuestLoginButton" Style="{StaticResource LinkButtonStyle}" Content="游客模式" Click="GuestLoginButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>